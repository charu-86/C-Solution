/* The popular pow method is something that everyon uses. But how does it actually work? Here's the background code for pow function */

#include<stdio.h>

double pow(int num, int p){
  int res = 1;
  while(n > 0){
    if(n % 2 != 0)
      res *= x;
    x *= x;
    n /=2;
  }
  return res;
}

int main(){
  return 0;
}
